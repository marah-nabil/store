server:
  port: 9001
  
spring:
  application:
    name: item
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/store?serverTimezone=UTC
    username: root
    password:
  
  sql:
    init:
      mode: always

